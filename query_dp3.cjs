const { DataService } = require("./src/firebase.js"); async function run() { const protocols = await DataService.load("damageProtocols") || []; const latest = protocols.sort((a,b) => new Date(b.createdAt||0).getTime() - new Date(a.createdAt||0).getTime()).slice(0, 5); latest.forEach((p, idx) => { console.log("--- Record " + (idx+1) + " ---"); console.log("protocolId: " + p.protocolId); console.log("vehicleId: " + p.vehicleId); console.log("createdBy: " + p.createdBy); console.log("createdAt: " + p.createdAt); }); } run();